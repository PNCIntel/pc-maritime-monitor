# v3.0.5 — Link Button Styling Fix

Fixed Streamlit external-link buttons such as `Open source` so they no longer appear as white controls on the dark interface.

The buttons now use:
- dark charcoal background
- P&C gold text
- muted border
- gold hover state
- no white fill

Applied to both `app.py` and `pc_intelligence_app.py`.
